'use babel';

import CopyFilepathWithLineNumbersView from '../lib/copy-filepath-with-line-numbers-view';

describe('CopyFilepathWithLineNumbersView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
